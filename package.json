{
    "name": "pland-legacyremotecallapi",
    "version": "0.7.0",
    "description": "PLand Legacy Remote Call API",
    "scripts": {
        "build": "npm run build:esm && npm run build:cjs",
        "build:esm": "tsc -p tsconfig.esm.json",
        "build:cjs": "tsc -p tsconfig.cjs.json",
        "clean": "rimraf bin/PLand-LegacyRemoteCallApi/lib"
    },
    "devDependencies": {
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=14.0.0"
    }
}